<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <title>Document</title>

    <style>
        body {
            background-image: url("images/alps_darkened.png");
            background-size: cover;
            font: 20px "Montserrat", sans-serif;
            color: #f5f6f7;
        }

        .header {
            margin-top: 8%;
        }

        .btn {
            margin-top: 8%;
            width: 200px;
            height: 100px;
            background-color: rgba(255, 255, 255, 0.5);
            font-size: 1.5em;
            border-color: rgba(255, 255, 255, 0.5);
        }

        .btn:active, .btn-primary:active:focus, .btn-primary:active:hover  {
            background-color: rgba(0, 0, 0, 0.9);
            border-color: rgba(0, 0, 0, 0.9);
            color: rgba(255, 255, 255);
        }

        .btn:hover {
            background-color: rgba(0, 0, 0, 0.5);
            border-color: rgba(0, 0, 0, 0.5);
            color: rgba(255, 255, 255);
        }

        .btn-left {
            margin-right: 5%;
        }

        .btn-right {
            margin-left: 5%;
        }

    </style>

    <script>
        const take_to_register = () => {
            window.open("http://localhost:3000/register", "_blank");
        }

        const take_to_signin = () => {
            window.open("http://localhost:3000/login", "_blank");
        }

    </script>
</head>
<body>
    <div class="container text-center header">
        <h1>Portfolio Wizard</h1>

        <h6>Show them the best of yourself</h6>
    </div>

    <div class="container text-center">
        <button class="btn btn-primary btn-left" onclick="take_to_signin()">Sign In</button>

        <button class="btn btn-primary btn-right" onclick="take_to_register()">Register</button>
    </div>
    
</body>
</html>